class ReportGenerator:
    """Generates human-readable reports from experiment and run data."""

    def generate_run_summary(self, run_id: int) -> str:
        return f"Run {run_id} summary placeholder."
